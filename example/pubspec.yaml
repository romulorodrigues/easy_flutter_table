name: example
description: Example app for testing easy_flutter_table
publish_to: "none" # Isso impede o Flutter de tentar publicar o exemplo no pub.dev

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  easy_flutter_table:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
